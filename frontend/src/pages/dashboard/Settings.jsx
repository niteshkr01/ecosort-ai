import React from "react";import {useNavigate} from "react-router-dom";import {useAuth} from "../../App.jsx";import Icon from "../../components/Icons.jsx";
export default function Settings(){const{user,logout}=useAuth();const nav=useNavigate();const initials=(user?.name||"N").split(" ").map(x=>x[0]).slice(0,2).join("").toUpperCase();return <div className="inner-page"><div className="page-heading"><div><div className="eyebrow">ACCOUNT CONTROL</div><h1>Settings</h1><p>Manage your EcoSort AI profile and session.</p></div></div><div className="settings-layout"><section className="panel profile-panel"><div className="profile-cover"/><div className="profile-main"><div className="profile-avatar">{initials}</div><h2>{user?.name||"Nitesh Kumar"}</h2><p>{user?.email}</p><span className="verified-pill"><Icon name="check" size={13}/> Account active</span></div><div className="settings-list"><div><span>Account type</span><strong>Student</strong></div><div><span>Platform</span><strong>EcoSort AI</strong></div><div><span>Classification engine</span><strong>Rule-based AI engine</strong></div></div></section><section className="panel"><h2>Session</h2><p className="panel-muted">Your authentication session is stored securely in this browser.</p><button className="logout-btn" onClick={()=>{logout();nav("/")}}><Icon name="logout" size={18}/> Log out</button></section></div></div>}
